import { Validatorsestudiante } from './validatorsestudiante';

describe('Validatorsestudiante', () => {
  it('should create an instance', () => {
    expect(new Validatorsestudiante()).toBeTruthy();
  });
});
